# My Global Coding Rules

## 🚫 Forbidden Patterns
- Never use `print()` for debugging in Python; use the `logging` module.
- Do not use `var` in JavaScript; use `const` or `let`.
- Never commit `.env` files.

## ⚠️ Preferences
- I prefer list comprehensions over `map()`/`filter()` in Python.
- Always add type hints to Python function arguments.
